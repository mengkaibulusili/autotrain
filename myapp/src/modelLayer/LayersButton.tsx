import { Flatten } from "./Layers";
export let ModelLayersView = (layers: Array<any>) => {
  let layersView = layers.map((layer: any) => {});

  return layersView;
};

export let FlattenButton = (layer: Flatten, f: Function) => {};

export let DenseButton = (layer: Flatten, f: Function) => {};
